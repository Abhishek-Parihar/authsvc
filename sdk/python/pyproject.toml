[project]
name = "authsvc-client"
version = "0.2.0"
description = "Python JWKS client for authsvc"
requires-python = ">=3.10"
dependencies = [
  "PyJWT>=2.8",
  "cryptography>=42",
  "requests>=2.31",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["authsvc_client*"]
